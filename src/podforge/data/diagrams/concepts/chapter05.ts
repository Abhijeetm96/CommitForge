import type { KubeConcept } from '../../topics/types';
import type { TopicFlowDiagramData } from '../../../components/diagrams/kubeDiagramTypes';

export const CHAPTER_05_CONCEPTS: Record<string, (c: KubeConcept) => TopicFlowDiagramData> = {
  // 5.1 Why Networking Is Needed
  'c-why-networking-needed': (c) => ({
    chapterNumber: 5,
    chapterTitle: 'Services & Networking',
    conceptNumber: c.number,
    conceptTitle: c.title,
    architectureType: 'Ephemeral Pod IP Churn vs Stable Service Abstraction',
    architecturalSummary:
      'Contrasts raw volatile Pod networking with Kubernetes Service discovery: Pods are ephemeral and receive new random IPs upon every restart, requiring the stable virtual VIP and DNS abstraction.',
    blocks: [
      {
        id: 'c51-frontend',
        label: 'Frontend Client Pod',
        sublabel: 'Web Client / Microservice A',
        category: 'client',
        icon: 'terminal',
        portOrProtocol: 'DNS Query :53',
        statusText: 'Caller',
        details: {
          role: 'Initiates API calls to backend services without knowing physical backend host IP addresses.',
          processName: 'frontend container',
          cliDiagnostic: 'kubectl exec -it <frontend> -- curl backend-svc',
          configLocation: '/etc/resolv.conf',
          keyInsight: 'Resolves service name via cluster DNS rather than hardcoding volatile pod IP addresses.',
        },
      },
      {
        id: 'c51-dns',
        label: 'CoreDNS Service Discovery',
        sublabel: 'backend-svc.default.svc.cluster.local',
        category: 'network',
        icon: 'network',
        portOrProtocol: ':53 UDP/TCP',
        statusText: 'Resolves to VIP',
        details: {
          role: 'Watches Services and translates service domain names into stable virtual ClusterIP addresses.',
          processName: 'coredns',
          cliDiagnostic: 'kubectl get svc -n kube-system kube-dns',
          configLocation: '/etc/coredns/Corefile',
          keyInsight: 'Returns immutable Virtual IP (VIP), shielding clients from backend pod restarts.',
        },
      },
      {
        id: 'c51-service-vip',
        label: 'Stable Virtual IP (ClusterIP)',
        sublabel: '10.96.0.100 (Virtual VIP)',
        category: 'control-plane',
        icon: 'shield',
        portOrProtocol: 'Kernel Netfilter VIP',
        statusText: 'Immutable VIP',
        details: {
          role: 'Permanent virtual IP that never changes throughout the entire lifecycle of the service.',
          processName: 'kube-proxy iptables/IPVS',
          cliDiagnostic: 'kubectl get svc backend-svc',
          configLocation: 'spec.clusterIP',
          keyInsight: 'Has no physical network interface; exists purely as kernel packet rewriting rules.',
        },
      },
      {
        id: 'c51-ephemeral-pods',
        label: 'Ephemeral Backend Pods',
        sublabel: 'Pod A (10.244.1.5) -> Crash -> Pod B (10.244.2.19)',
        category: 'node',
        icon: 'box',
        portOrProtocol: 'Dynamic Pod IPs',
        statusText: 'Volatile IPs',
        details: {
          role: 'Backend application pods that are frequently terminated, scaled, or rescheduled with brand new IP addresses.',
          processName: 'backend service',
          cliDiagnostic: 'kubectl get pods -l app=backend -o wide',
          configLocation: 'metadata.labels: app=backend',
          keyInsight: 'Directly calling Pod IPs leads to instant outage when pods are replaced during rolling deploys.',
        },
      },
    ],
    connections: [
      { from: 'c51-frontend', to: 'c51-dns', label: '1. Resolves backend-svc DNS name', protocol: 'DNS Query', stepNumber: 1 },
      { from: 'c51-dns', to: 'c51-frontend', label: '2. Returns stable ClusterIP (10.96.0.100)', protocol: 'DNS A-Record', stepNumber: 2 },
      { from: 'c51-frontend', to: 'c51-service-vip', label: '3. Sends HTTP traffic to stable VIP', protocol: 'TCP :80', stepNumber: 3 },
      { from: 'c51-service-vip', to: 'c51-ephemeral-pods', label: '4. Kernel DNAT routes packet to currently healthy Pod IP', protocol: 'iptables DNAT', stepNumber: 4 },
    ],
    steps: [
      {
        step: 1,
        title: 'Service Name Resolution',
        summary: 'Frontend client resolves backend-svc domain name against CoreDNS.',
        activeBlockIds: ['c51-frontend', 'c51-dns'],
        activeConnectionIdxs: [0],
        detailExplanation: {
          simpleWords: 'Your frontend calls backend-svc instead of guessing an unpredictable IP address.',
          technicalMechanics: 'Container resolver queries nameserver in /etc/resolv.conf; CoreDNS matches service record in default namespace.',
        },
        kubectlTrace: 'kubectl exec deploy/frontend -- nslookup backend-svc',
      },
      {
        step: 2,
        title: 'Virtual IP Return',
        summary: 'CoreDNS responds with stable, immutable ClusterIP virtual address (10.96.0.100).',
        activeBlockIds: ['c51-dns', 'c51-frontend'],
        activeConnectionIdxs: [1],
        detailExplanation: {
          simpleWords: 'The DNS returns one permanent virtual telephone number that never changes.',
          technicalMechanics: 'CoreDNS returns A-record for backend-svc pointing to virtual IP allocated from service-cluster-ip-range CIDR.',
        },
        kubectlTrace: 'kubectl get svc backend-svc -o jsonpath="{.spec.clusterIP}"',
      },
      {
        step: 3,
        title: 'Virtual IP Packet Dispatch',
        summary: 'Frontend transmits packets addressed to the ClusterIP virtual destination.',
        activeBlockIds: ['c51-frontend', 'c51-service-vip'],
        activeConnectionIdxs: [2],
        detailExplanation: {
          simpleWords: 'Traffic heads towards the virtual number. The operating system kernel intercepts it in flight.',
          technicalMechanics: 'Linux kernel netfilter intercepts SYN packet before egress; matches KUBE-SERVICES iptables chain.',
        },
        kubectlTrace: 'iptables-save | grep backend-svc',
      },
      {
        step: 4,
        title: 'Kernel DNAT Translation to Live Pod',
        summary: 'Kernel rewrites destination IP to a currently active, healthy backend pod IP.',
        activeBlockIds: ['c51-service-vip', 'c51-ephemeral-pods'],
        activeConnectionIdxs: [3],
        detailExplanation: {
          simpleWords: 'Even if the old pod died 5 seconds ago, the kernel forwards the call to the brand new living pod.',
          technicalMechanics: 'iptables/IPVS executes Destination NAT (DNAT), replacing 10.96.0.100 with active endpoint IP 10.244.2.19.',
        },
        kubectlTrace: 'kubectl get endpointslices -l kubernetes.io/service-name=backend-svc',
      },
    ],
  }),

  // 5.2 Kubernetes Services
  'c-k8s-services-clusterip': (c) => ({
    chapterNumber: 5,
    chapterTitle: 'Services & Networking',
    conceptNumber: c.number,
    conceptTitle: c.title,
    architectureType: 'Kubernetes Virtual Networking & Ingress Packet Path',
    architecturalSummary:
      'Details packet flow through Kubernetes networking: ClusterIP virtual IP allocation, kube-proxy netfilter programming (iptables PREROUTING / IPVS hash tables), and load balancing across active pod endpoints.',
    blocks: [
      {
        id: 'c52-client',
        label: 'Client / Calling Pod',
        sublabel: 'curl 10.96.0.100:80',
        category: 'client',
        icon: 'terminal',
        portOrProtocol: 'TCP SYN Packet',
        statusText: 'Originator',
        details: {
          role: 'Generates client connection targeted at Service ClusterIP virtual IP.',
          processName: 'curl / client app',
          cliDiagnostic: 'kubectl exec -it <pod> -- curl -I 10.96.0.100',
          configLocation: 'Client Socket',
          keyInsight: 'Does not know or care which worker node or pod actually serves the connection.',
        },
      },
      {
        id: 'c52-kube-proxy',
        label: 'kube-proxy Daemon',
        sublabel: 'iptables / IPVS Controller',
        category: 'network',
        icon: 'activity',
        portOrProtocol: 'Kernel Netlink Sync',
        statusText: 'Syncing Rules',
        details: {
          role: 'Watches API Server for Service and EndpointSlice changes; writes iptables or IPVS rules on every node.',
          processName: 'kube-proxy',
          cliDiagnostic: 'kubectl logs -n kube-system -l k8s-app=kube-proxy',
          configLocation: '/var/lib/kube-proxy/config.conf',
          keyInsight: 'In IPVS mode, uses ipset hash tables scaling to tens of thousands of services with O(1) lookup.',
        },
      },
      {
        id: 'c52-netfilter',
        label: 'Linux Netfilter / iptables',
        sublabel: 'KUBE-SERVICES -> KUBE-SVC-*',
        category: 'node',
        icon: 'shield',
        portOrProtocol: 'Kernel Packet Engine',
        statusText: 'DNAT Engine',
        details: {
          role: 'Intercepts incoming packets, randomly selects an active backend pod, and rewrites packet header via DNAT.',
          processName: 'Linux Kernel netfilter',
          cliDiagnostic: 'iptables -t nat -L KUBE-SERVICES -n -v',
          configLocation: '/proc/net/ip_conntrack',
          keyInsight: 'Uses random probability matching (statistic mode random) to distribute traffic evenly across endpoints.',
        },
      },
      {
        id: 'c52-endpoint-a',
        label: 'Backend Pod A',
        sublabel: '10.244.1.5:8080 (Ready)',
        category: 'runtime',
        icon: 'box',
        portOrProtocol: 'Pod IP / veth',
        statusText: 'Healthy 50%',
        details: {
          role: 'Worker pod receiving and servicing forwarded HTTP requests.',
          processName: 'nginx / node worker',
          cliDiagnostic: 'kubectl logs pod-a',
          configLocation: 'spec.containers',
          keyInsight: 'Receives the original client source IP unless NodePort externalTrafficPolicy: Cluster SNAT is active.',
        },
      },
      {
        id: 'c52-endpoint-b',
        label: 'Backend Pod B',
        sublabel: '10.244.2.8:8080 (Ready)',
        category: 'runtime',
        icon: 'box',
        portOrProtocol: 'Pod IP / veth',
        statusText: 'Healthy 50%',
        details: {
          role: 'Second worker pod providing high availability and parallel request processing.',
          processName: 'nginx / node worker',
          cliDiagnostic: 'kubectl logs pod-b',
          configLocation: 'spec.containers',
          keyInsight: 'If readiness probe fails, kube-proxy removes this endpoint from iptables within milliseconds.',
        },
      },
    ],
    connections: [
      { from: 'c52-client', to: 'c52-netfilter', label: '1. Emits TCP packet to ClusterIP 10.96.0.100:80', protocol: 'TCP SYN', stepNumber: 1 },
      { from: 'c52-kube-proxy', to: 'c52-netfilter', label: '2. Injects KUBE-SVC probability chains', protocol: 'iptables-restore', stepNumber: 2 },
      { from: 'c52-netfilter', to: 'c52-endpoint-a', label: '3. 50% probability: DNAT rewrite to 10.244.1.5', protocol: 'Kernel DNAT', stepNumber: 3 },
      { from: 'c52-netfilter', to: 'c52-endpoint-b', label: '4. 50% probability: DNAT rewrite to 10.244.2.8', protocol: 'Kernel DNAT', stepNumber: 3 },
    ],
    steps: [
      {
        step: 1,
        title: 'Virtual IP Packet Initiation',
        summary: 'Calling pod sends packet directed to Service virtual IP 10.96.0.100.',
        activeBlockIds: ['c52-client', 'c52-netfilter'],
        activeConnectionIdxs: [0],
        detailExplanation: {
          simpleWords: 'A computer sends a request to the Service virtual address.',
          technicalMechanics: 'Socket connects to 10.96.0.100:80; route lookup routes packet through node loopback or eth0.',
        },
        kubectlTrace: 'kubectl get svc web-service',
      },
      {
        step: 2,
        title: 'kube-proxy Netfilter Rule Synchronization',
        summary: 'kube-proxy keeps kernel routing tables in sync with cluster EndpointSlice state.',
        activeBlockIds: ['c52-kube-proxy', 'c52-netfilter'],
        activeConnectionIdxs: [1],
        detailExplanation: {
          simpleWords: 'kube-proxy writes the road signs into the computer kernel memory.',
          technicalMechanics: 'kube-proxy receives EndpointSlice update and writes iptables chains: KUBE-SVC-XXX with probability rules.',
        },
        kubectlTrace: 'kubectl get endpoints web-service',
      },
      {
        step: 3,
        title: 'Kernel Netfilter DNAT Load Balancing',
        summary: 'Linux netfilter intercepts the packet and translates virtual VIP to an active pod endpoint IP.',
        activeBlockIds: ['c52-netfilter', 'c52-endpoint-a', 'c52-endpoint-b'],
        activeConnectionIdxs: [2, 3],
        detailExplanation: {
          simpleWords: 'The kernel flips a coin and redirects the message to Pod A or Pod B.',
          technicalMechanics: 'iptables matches rule with -m statistic --mode random --probability 0.50 and rewrites destination IP/port.',
        },
        kubectlTrace: 'kubectl describe endpointslices web-service',
      },
    ],
  }),

  // 5.3 External Access & Ingress
  'c-external-access-ingress': (c) => ({
    chapterNumber: 5,
    chapterTitle: 'Services & Networking',
    conceptNumber: c.number,
    conceptTitle: c.title,
    architectureType: 'Ingress Controller, Host/Path Routing & TLS Termination',
    architecturalSummary:
      'Traces external client ingress traffic: Cloud LoadBalancer entry, Ingress Controller (NGINX/Envoy) reverse proxy, TLS decryption, Host & Path routing, and upstream service forwarding.',
    blocks: [
      {
        id: 'c53-internet',
        label: 'Public Internet Client',
        sublabel: 'https://shop.example.com/checkout',
        category: 'external',
        icon: 'terminal',
        portOrProtocol: 'HTTPS :443 (TLS Encrypted)',
        statusText: 'External User',
        details: {
          role: 'Public web browser or mobile client issuing HTTPS requests to domain name.',
          processName: 'Browser / Mobile Client',
          cliDiagnostic: 'curl -iv https://shop.example.com/checkout',
          configLocation: 'Public DNS A-Record',
          keyInsight: 'Resolves shop.example.com to the public IP of the Cloud Load Balancer.',
        },
      },
      {
        id: 'c53-cloud-lb',
        label: 'Cloud Load Balancer (L4)',
        sublabel: 'AWS NLB / GCP External Passthrough',
        category: 'external',
        icon: 'server',
        portOrProtocol: 'Public IP :443 -> NodePort',
        statusText: 'Layer 4 TCP Pass',
        details: {
          role: 'High-capacity cloud load balancer distributing raw TCP traffic across cluster worker nodes.',
          processName: 'Cloud Provider LB',
          cliDiagnostic: 'kubectl get svc -n ingress-nginx ingress-nginx-controller',
          configLocation: 'Service type: LoadBalancer',
          keyInsight: 'Passes encrypted TCP traffic directly to Ingress Controller pods running on worker nodes.',
        },
      },
      {
        id: 'c53-ingress-ctrl',
        label: 'Ingress Controller (L7)',
        sublabel: 'ingress-nginx / Envoy Gateway',
        category: 'network',
        icon: 'activity',
        portOrProtocol: 'OpenSSL TLS Termination',
        statusText: 'Reverse Proxy',
        details: {
          role: 'Terminates TLS with certificate from secret, inspects HTTP Host and Path headers, and rewrites headers.',
          processName: 'nginx / envoy',
          cliDiagnostic: 'kubectl logs -n ingress-nginx -l app.kubernetes.io/name=ingress-nginx',
          configLocation: '/etc/nginx/nginx.conf',
          keyInsight: 'A single Ingress Controller can route hundreds of domain names and microservices behind one shared public IP.',
        },
      },
      {
        id: 'c53-tls-secret',
        label: 'TLS Certificate Secret',
        sublabel: 'tls.crt + tls.key (Cert-Manager)',
        category: 'security',
        icon: 'shield',
        portOrProtocol: 'kubernetes.io/tls',
        statusText: 'Valid Certificate',
        details: {
          role: 'Stores RSA/ECDSA public cert and private key mounted dynamically by Ingress Controller.',
          processName: 'kube-apiserver secret',
          cliDiagnostic: 'kubectl get secret example-tls -o yaml',
          configLocation: 'spec.tls[0].secretName',
          keyInsight: 'Cert-manager automates Let\'s Encrypt certificate renewal before expiration.',
        },
      },
      {
        id: 'c53-backend-svc',
        label: 'Target Backend Service',
        sublabel: 'checkout-service:8080 (ClusterIP)',
        category: 'node',
        icon: 'box',
        portOrProtocol: 'Internal HTTP Forward',
        statusText: 'Servicing Request',
        details: {
          role: 'Internal microservice handling checkout logic without needing public IP address.',
          processName: 'checkout app pod',
          cliDiagnostic: 'kubectl get ingress',
          configLocation: 'spec.rules.http.paths',
          keyInsight: 'NGINX ingress bypasses kube-proxy and forwards packets directly to Pod IPs for minimal latency.',
        },
      },
    ],
    connections: [
      { from: 'c53-internet', to: 'c53-cloud-lb', label: '1. HTTPS request to shop.example.com/checkout', protocol: 'Public Internet :443', stepNumber: 1 },
      { from: 'c53-cloud-lb', to: 'c53-ingress-ctrl', label: '2. Forwards TCP connection to Ingress Controller', protocol: 'NodePort / ProxyProtocol', stepNumber: 2 },
      { from: 'c53-tls-secret', to: 'c53-ingress-ctrl', label: '3. Supplies TLS private key for SSL handshake', protocol: 'In-Memory Decrypt', stepNumber: 2 },
      { from: 'c53-ingress-ctrl', to: 'c53-backend-svc', label: '4. Routes path /checkout to checkout-service endpoints', protocol: 'HTTP/1.1 Upstream KeepAlive', stepNumber: 3 },
    ],
    steps: [
      {
        step: 1,
        title: 'Public HTTPS Request Arrival',
        summary: 'User browser sends HTTPS request reaching the Cloud Provider Load Balancer.',
        activeBlockIds: ['c53-internet', 'c53-cloud-lb'],
        activeConnectionIdxs: [0],
        detailExplanation: {
          simpleWords: 'A customer visits your store from home. Their browser calls the cloud front gate.',
          technicalMechanics: 'DNS resolves hostname to cloud provider L4 VIP; load balancer opens TCP connection to worker node.',
        },
        kubectlTrace: 'curl -kv https://shop.example.com/checkout',
      },
      {
        step: 2,
        title: 'TLS Decryption & Ingress Inspection',
        summary: 'Ingress Controller decrypts TLS using secret and evaluates HTTP Host and Path headers.',
        activeBlockIds: ['c53-cloud-lb', 'c53-ingress-ctrl', 'c53-tls-secret'],
        activeConnectionIdxs: [1, 2],
        detailExplanation: {
          simpleWords: 'The Ingress Controller unlocks the encryption using your digital certificate and inspects the URL path.',
          technicalMechanics: 'NGINX terminates SSL using keys from secretName: example-tls; matches server_name shop.example.com and location /checkout.',
        },
        kubectlTrace: 'kubectl describe ingress shop-ingress',
      },
      {
        step: 3,
        title: 'Upstream Direct-to-Pod Forwarding',
        summary: 'Controller selects healthy checkout-service pod from EndpointSlice and proxies request.',
        activeBlockIds: ['c53-ingress-ctrl', 'c53-backend-svc'],
        activeConnectionIdxs: [3],
        detailExplanation: {
          simpleWords: 'The request is handed straight to the checkout pod inside the cluster.',
          technicalMechanics: 'ingress-nginx uses dynamic Lua balancer to stream request directly to pod IP 10.244.3.42:8080.',
        },
        kubectlTrace: 'kubectl logs -n ingress-nginx -l app.kubernetes.io/name=ingress-nginx --tail=50',
      },
    ],
  }),

  // 5.4 Load Balancing & Endpoints
  'c-load-balancing-endpoints': (c) => ({
    chapterNumber: 5,
    chapterTitle: 'Services & Networking',
    conceptNumber: c.number,
    conceptTitle: c.title,
    architectureType: 'EndpointSlice Controller & High-Scale Load Balancing Topology',
    architecturalSummary:
      'Details how the EndpointSlice Controller solves traditional Endpoints scaling bottlenecks by chunking pods into slices (max 100 endpoints per slice), tracking readiness conditions, and distributing traffic efficiently.',
    blocks: [
      {
        id: 'c54-pods',
        label: 'Pod Fleet (1000+ Replicas)',
        sublabel: 'Large Scale Deployments',
        category: 'node',
        icon: 'box',
        portOrProtocol: 'Pod IPs (10.244.x.x)',
        statusText: 'Dynamic Scaling',
        details: {
          role: 'Fleet of pods running at scale; pods frequently start, stop, and transition readiness states.',
          processName: 'workload pods',
          cliDiagnostic: 'kubectl get pods -l app=large-scale -o wide | wc -l',
          configLocation: 'spec.replicas: 1000',
          keyInsight: 'In legacy Endpoints, a single pod change required rewriting a 10MB monolithic object to all nodes.',
        },
      },
      {
        id: 'c54-controller',
        label: 'EndpointSlice Controller',
        sublabel: 'kube-controller-manager',
        category: 'control-plane',
        icon: 'cpu',
        portOrProtocol: 'discovery.k8s.io/v1',
        statusText: 'Chunking Engine',
        details: {
          role: 'Aggregates pod network endpoints and batches them into multiple small EndpointSlice objects (up to 100 endpoints each).',
          processName: 'endpointslice-controller',
          cliDiagnostic: 'kubectl get endpointslices -l kubernetes.io/service-name=large-svc',
          configLocation: '/etc/kubernetes',
          keyInsight: 'Reduces network traffic to API server by 99% during massive cluster scaling events.',
        },
      },
      {
        id: 'c54-slices',
        label: 'EndpointSlices (0..N)',
        sublabel: 'large-svc-1, large-svc-2 (100 IPs each)',
        category: 'network',
        icon: 'layers',
        portOrProtocol: '100 Endpoints / Slice',
        statusText: 'Chunked State',
        details: {
          role: 'Stores endpoint addresses, ports, readiness conditions, and topology zone hints (topology.kubernetes.io/zone).',
          processName: 'EndpointSlice objects',
          cliDiagnostic: 'kubectl describe endpointslice large-svc-1',
          configLocation: 'discovery.k8s.io API',
          keyInsight: 'Supports Topology Aware Routing to keep traffic within the same availability zone for lower latency.',
        },
      },
      {
        id: 'c54-proxy',
        label: 'kube-proxy IPVS Engine',
        sublabel: 'Kernel IPVS Hash Tables',
        category: 'node',
        icon: 'shield',
        portOrProtocol: 'IPVS rr / lc scheduler',
        statusText: 'O(1) Kernel Routing',
        details: {
          role: 'Consumes EndpointSlices and programs Linux IPVS hash tables on worker nodes.',
          processName: 'ipvsadm',
          cliDiagnostic: 'ipvsadm -ln',
          configLocation: '/proc/net/ip_vs',
          keyInsight: 'Provides O(1) performance regardless of whether there are 10 or 10,000 backend endpoints.',
        },
      },
    ],
    connections: [
      { from: 'c54-pods', to: 'c54-controller', label: '1. Pod state transitions streamed via Informer', protocol: 'API Server Watch', stepNumber: 1 },
      { from: 'c54-controller', to: 'c54-slices', label: '2. Updates strictly the affected 100-endpoint slice', protocol: 'Slice Mutation', stepNumber: 2 },
      { from: 'c54-slices', to: 'c54-proxy', label: '3. Worker node kube-proxy syncs IPVS hash entry', protocol: 'Informer Watch', stepNumber: 3 },
    ],
    steps: [
      {
        step: 1,
        title: 'Pod Health Transition Detection',
        summary: 'A pod becomes ready or crashes; API server broadcasts state delta to EndpointSlice controller.',
        activeBlockIds: ['c54-pods', 'c54-controller'],
        activeConnectionIdxs: [0],
        detailExplanation: {
          simpleWords: 'A worker pod wakes up or fails its health check.',
          technicalMechanics: 'Kubelet updates Pod.Status.Conditions[Type=Ready]; controller-manager Informer receives change.',
        },
        kubectlTrace: 'kubectl get pods -w',
      },
      {
        step: 2,
        title: 'Surgical Slice Chunk Update',
        summary: 'Controller updates only the specific EndpointSlice containing that pod, leaving other slices untouched.',
        activeBlockIds: ['c54-controller', 'c54-slices'],
        activeConnectionIdxs: [1],
        detailExplanation: {
          simpleWords: 'Instead of rewriting a giant telephone book, Kubernetes only updates one tiny page.',
          technicalMechanics: 'EndpointSlice controller mutates only the targeted discovery.k8s.io/v1 object, transmitting just a few kilobytes.',
        },
        kubectlTrace: 'kubectl get endpointslices',
      },
      {
        step: 3,
        title: 'O(1) IPVS Kernel Load Balancing',
        summary: 'kube-proxy updates Linux kernel IPVS hash tables, distributing traffic across endpoints.',
        activeBlockIds: ['c54-slices', 'c54-proxy'],
        activeConnectionIdxs: [2],
        detailExplanation: {
          simpleWords: 'The server hardware instantly updates its lookup table without slowing down incoming traffic.',
          technicalMechanics: 'kube-proxy adds or deletes real server entry in IPVS table using netlink socket with zero packet drop.',
        },
        kubectlTrace: 'ipvsadm -l -n',
      },
    ],
  }),

  // 5.5 Pod-to-Pod Networking
  'c-pod-to-pod-networking': (c) => ({
    chapterNumber: 5,
    chapterTitle: 'Services & Networking',
    conceptNumber: c.number,
    conceptTitle: c.title,
    architectureType: 'CNI Overlay Encapsulation & Cross-Node Packet Path',
    architecturalSummary:
      'Traces packet flow from Pod A on Node 1 to Pod B on Node 2: Container veth pair, CNI bridge / eBPF, VXLAN overlay encapsulation (UDP 4789), physical fabric transport, and destination decapsulation.',
    blocks: [
      {
        id: 'c55-pod-a',
        label: 'Pod A (Node 1)',
        sublabel: 'IP: 10.244.1.10 • eth0',
        category: 'runtime',
        icon: 'terminal',
        portOrProtocol: '10.244.1.10 -> 10.244.2.20',
        statusText: 'Transmitting',
        details: {
          role: 'Source application sending IP packet to Pod B located on a different physical worker host.',
          processName: 'app process',
          cliDiagnostic: 'kubectl exec pod-a -- ip route',
          configLocation: 'Pod Network Namespace',
          keyInsight: 'Kubernetes IP-per-pod model requires all pods to communicate without NAT.',
        },
      },
      {
        id: 'c55-veth-bridge',
        label: 'Virtual Ethernet (veth) Pair',
        sublabel: 'veth-pod <-> veth-host / cni0',
        category: 'network',
        icon: 'network',
        portOrProtocol: 'Linux veth pair',
        statusText: 'Host Bridge',
        details: {
          role: 'Virtual wire carrying packets out of the container network namespace into the host root network.',
          processName: 'veth driver',
          cliDiagnostic: 'ip link show type veth',
          configLocation: '/sys/class/net',
          keyInsight: 'Packets exiting container eth0 appear immediately on corresponding veth host interface.',
        },
      },
      {
        id: 'c55-cni-overlay',
        label: 'CNI Overlay Engine (VXLAN)',
        sublabel: 'Calico / Flannel / Cilium',
        category: 'node',
        icon: 'shield',
        portOrProtocol: 'UDP :4789 VXLAN Encapsulation',
        statusText: 'Encapsulating',
        details: {
          role: 'Wraps inner Pod IP packet inside an outer UDP packet addressed to Node 2 physical IP.',
          processName: 'vxlan.calico / flannel.1',
          cliDiagnostic: 'bridge fdb show',
          configLocation: '/etc/cni/net.d',
          keyInsight: 'Allows pod subnets to communicate across physical routers that have no knowledge of pod IPs.',
        },
      },
      {
        id: 'c55-phys-net',
        label: 'Physical Underlay Network',
        sublabel: 'Node 1 (192.168.1.10) -> Node 2 (192.168.1.20)',
        category: 'external',
        icon: 'server',
        portOrProtocol: 'Standard L2/L3 Fabric',
        statusText: 'Physical Routing',
        details: {
          role: 'Physical datacenter switches or cloud VPC network transporting standard UDP packets between hosts.',
          processName: 'Host NIC eth0',
          cliDiagnostic: 'ip route get 192.168.1.20',
          configLocation: 'Physical Router',
          keyInsight: 'Physical network sees only standard traffic between Node 1 and Node 2 IP addresses.',
        },
      },
      {
        id: 'c55-pod-b',
        label: 'Pod B (Node 2)',
        sublabel: 'IP: 10.244.2.20 • eth0',
        category: 'runtime',
        icon: 'box',
        portOrProtocol: 'Receiving Socket',
        statusText: 'Packet Delivered',
        details: {
          role: 'Destination container receiving unmodified original IP packet from Pod A.',
          processName: 'destination server',
          cliDiagnostic: 'kubectl exec pod-b -- tcpdump -i eth0 -n',
          configLocation: 'Pod Network Namespace',
          keyInsight: 'Sees Pod A true IP (10.244.1.10) in packet header without any intermediate NAT.',
        },
      },
    ],
    connections: [
      { from: 'c55-pod-a', to: 'c55-veth-bridge', label: '1. Injects packet into veth pair towards default gateway', protocol: 'veth Pipe', stepNumber: 1 },
      { from: 'c55-veth-bridge', to: 'c55-cni-overlay', label: '2. Routing table directs packet to VXLAN tunnel device', protocol: 'Kernel Routing', stepNumber: 2 },
      { from: 'c55-cni-overlay', to: 'c55-phys-net', label: '3. Encapsulates in UDP :4789; travels over physical wire', protocol: 'VXLAN Outer Packet', stepNumber: 3 },
      { from: 'c55-phys-net', to: 'c55-pod-b', label: '4. Node 2 decapsulates outer UDP; delivers to Pod B eth0', protocol: 'Decapsulation Delivery', stepNumber: 4 },
    ],
    steps: [
      {
        step: 1,
        title: 'Container Network Namespace Egress',
        summary: 'Pod A checks routing table and sends packet out eth0 across the virtual ethernet cable.',
        activeBlockIds: ['c55-pod-a', 'c55-veth-bridge'],
        activeConnectionIdxs: [0],
        detailExplanation: {
          simpleWords: 'Pod A writes a letter addressed to Pod B and drops it into its virtual mailbox.',
          technicalMechanics: 'Kernel passes packet from container eth0 to host veth interface via Linux virtual ethernet driver.',
        },
        kubectlTrace: 'kubectl exec pod-a -- ping -c 1 10.244.2.20',
      },
      {
        step: 2,
        title: 'Host Route & VXLAN Tunnel Entry',
        summary: 'Node 1 kernel route lookup identifies destination pod subnet lives on Node 2; passes to VXLAN device.',
        activeBlockIds: ['c55-veth-bridge', 'c55-cni-overlay'],
        activeConnectionIdxs: [1],
        detailExplanation: {
          simpleWords: 'The host post office looks at the destination and realizes this letter belongs on a different computer.',
          technicalMechanics: 'Linux kernel routing table sends packet to flannel.1 or vxlan.calico tunnel device.',
        },
        kubectlTrace: 'ip route show | grep 10.244',
      },
      {
        step: 3,
        title: 'Overlay Encapsulation & Physical Transport',
        summary: 'CNI wraps packet in outer UDP header (port 4789) and sends over physical datacenter network.',
        activeBlockIds: ['c55-cni-overlay', 'c55-phys-net'],
        activeConnectionIdxs: [2],
        detailExplanation: {
          simpleWords: 'The letter is put inside a bigger shipping envelope addressed to Computer 2 and mailed over the wire.',
          technicalMechanics: 'VXLAN VTEP adds outer IP header (Src: Node 1 IP, Dst: Node 2 IP) and 8-byte VXLAN header.',
        },
        kubectlTrace: 'tcpdump -i eth0 port 4789 -n',
      },
      {
        step: 4,
        title: 'Decapsulation & Pod B Reception',
        summary: 'Node 2 decapsulates outer UDP packet and injects original inner packet into Pod B eth0.',
        activeBlockIds: ['c55-phys-net', 'c55-pod-b'],
        activeConnectionIdxs: [3],
        detailExplanation: {
          simpleWords: 'Computer 2 tears off the shipping envelope and hands the pristine original letter directly to Pod B.',
          technicalMechanics: 'Kernel strips VXLAN header and routes raw IP packet across veth pair into Pod B network namespace.',
        },
        kubectlTrace: 'kubectl logs pod-b',
      },
    ],
  }),
};
